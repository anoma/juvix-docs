module Package;

import PackageDescription.V2 open;

package : Package :=
  defaultPackage
    {name := "Judoc";
     version := mkVersion 0 1 0;
     dependencies := [];
     main := just "Main.juvix"};
